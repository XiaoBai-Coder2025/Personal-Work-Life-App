# 个人工作生活 App · 产品需求文档（PRD）

日期：2026-09-22
版本：第一版（v1）
状态：已确认，作为后续开发依据

相关文档：

- [产品说明](docs/产品说明.md)：面向介绍的精简版
- [第一版设计](docs/superpowers/specs/2026-09-21-personal-work-life-app-design.md)：技术方案
- [开发计划](docs/开发计划.md)：阶段划分与每阶段任务
- [实现计划 1](docs/superpowers/plans/2026-09-21-01-骨架与数据层.md)：骨架与数据层的实施步骤

优先级约定：本文件是后续开发的唯一依据。若与其他文档冲突，以本文件为准；技术实现细节以设计文档为准。

---

## 1. 产品目标和使用场景

### 1.1 产品目标

做一款只在本机运行的桌面端个人工作生活管理工具，把"当天要做的事"和"长期推进的事"放进同一套数据：

- 每天打开就能知道今天该做什么，不需要先整理待办
- 长期任务以"阶段 × 每天时段"的方式落地，能看出还剩多少投入、会不会来不及
- 自动生成的内容（智能排布、AI、文件导入）一律先成为草稿，经使用者采纳后才写入正式数据
- 数据保存在本机文件中，不依赖账号、云端与部署

### 1.2 使用场景

| 场景 | 使用方式 |
| --- | --- |
| 早上开始一天 | 打开首页总览，看即将截止、今天的计划与给你的建议 |
| 到点开始一件事 | 今日计划弹出询问，选择"是""稍等一会""不做了"，系统记录实际起止时间 |
| 临时想记一句 | 首页快速备忘直接输入 |
| 推进长期任务 | 任务追踪中查看阶段进度、剩余投入，把今天这一段排进计划 |
| 事项变多、时间冲突 | 日历日程中查看轴线图，直接改起止时间，重叠会被标出 |
| 需要出门 | 地图工具中查看路线、顺序与出发时间，必要时切换节假日估算 |
| 让工具帮忙安排 | 底部 AI 输入框提问，或对模糊目标使用 AI 智能安排 |
| 换电脑或担心丢失 | 数据与设置中导出备份，需要时导入还原 |

### 1.3 运行前提

- 运行环境：本机独立应用窗口（复用系统已有的 Edge/Chrome 内核，不带浏览器地址栏与标签页），启动即最大化
- 使用人数：单人
- 联网范围：仅地图工具与 AI 助手需要联网，其余功能离线可用
- 密钥：高德 JS Key 与 AI Key 由使用者自行提供，保存在本机

---

## 2. 整体页面框架和导航结构

### 2.1 窗口与整体布局

应用为单窗口结构，自上而下分四层：

1. 顶栏：当前模块名称、日期与星期、天气、明暗切换（亮 / 暗 / 跟随系统）、当前配色名称
2. 内容区：当前模块主体，模块内过长时在内容区内滚动
3. 底部常驻 AI 输入框：在所有模块下方固定显示，提问方式与快捷问题随当前模块变化

左侧为导航栏（七项），宽屏下常驻；也可切换为顶部标签形式。

### 2.2 导航结构

导航顺序固定为：

1. 首页总览
2. 今日计划
3. 日历日程
4. 任务追踪
5. 地图工具
6. AI 助手
7. 数据与设置

### 2.3 外观与窗口要求

- 窗口铺满屏幕，启动即最大化；切换模块时窗口高度不发生变化
- 明暗三态：亮、暗、跟随系统
- 配色五套：默认蓝、墨绿、暖橙、夜紫、跟随天气；"跟随天气"按当地天气取用对应配色
- 外观设置保存在本机，重启后仍然生效

---

## 3. 首页的内容结构

首页只做聚合与速记，不做深度编辑。结构如下：

| 位置 | 区块 | 内容 |
| --- | --- | --- |
| 顶部横幅 | 草稿提示 | 待确认草稿数量、展开所有 / 折叠所有 |
| 左列第一块 | 即将截止 | 按日期排序的临近截止事项，可跳转到来源模块 |
| 左列第二块 | 今天的计划 | 当天时间顺序的安排摘要 |
| 左列第三块 | 长周期任务 | 任务名称、进度、交付日、剩余投入小时数、下一天的时间段 |
| 右列第一块 | 给你的建议 | 结合身份、地点、天气与突发情况的提示，每条注明依据 |
| 右列第二块 | 快速备忘 | 输入框（回车保存）与最近备忘，可逐条删除 |
| 右列第三块 | 今天要去的地方 | 当天的地点与路程耗时、建议出发时间 |

交互要求：每块都是可折叠抽屉，支持单独展开、单独收起，以及全部展开与全部收起。

---

## 4. 每个模块的用途

| 模块 | 用途 |
| --- | --- |
| 首页总览 | 一屏了解当天与近期状态，并随手记录 |
| 今日计划 | 按时间顺序呈现当天安排，并在预定时间询问与记录实际执行情况 |
| 日历日程 | 呈现长期节奏与时间冲突，可调整任意一条日程的起止时间 |
| 任务追踪 | 长周期任务的推进台账，回答"还剩多少投入、会不会来不及" |
| 地图工具 | 空间视角，解决"去哪、怎么去、几点出门" |
| AI 助手 | 读取本机数据回答问题、生成草稿，并按当前模块提供对应提问 |
| 数据与设置 | 身份、密钥、外观与数据管理（备份与恢复） |

---

## 5. 每个模块第一版最必要的数据和操作

### 5.1 首页总览

数据：今日计划摘要、即将截止事项、长周期任务摘要、备忘、当天地点与路程、建议条目及其依据标签。

操作：展开 / 收起单个区块、全部展开 / 全部收起、新增备忘、删除备忘、跳转到对应模块。

### 5.2 今日计划

数据：当天的时间轴条目（起止时间、标题、类型：课表 / 安排 / 路上 / 休息、地点、关联的任务与时段、完成状态）、待确认草稿、实际起止时间与实际用时。

操作：采纳 / 忽略草稿、手动添加一条、删除一条、勾选完成、按顺序顺延、"是 / 稍等一会 / 不做了"三个执行选项（稍等一会可填推迟分钟数）。

### 5.3 日历日程

数据：日程条目（日期、起止时间、标题、类型）、跨天标记、重叠信息。

操作：切换周（上一周 / 本周 / 下一周）、点选日期、新增一条、修改起止日期与时间、删除一条、把某条排进今日计划。

### 5.4 任务追踪

数据：每条任务的任务名、起止日期、交付日、阶段列表与当前阶段、每天投入上限、各阶段下的时段（日期、几点到几点、完成状态）、风险与依赖说明；阶段模板（咨询项目 / 比赛 / 课程作业 / 备考）。

操作：新建任务（选模板或自填阶段，自动铺开工作日并分配到各阶段）、删除任务（先确认并提示连带影响）、增加 / 删除 / 切换阶段、修改每个时段的起止时间、勾选时段完成、给某个阶段增加时段、调整每天投入上限、让 AI 按交付日重新安排这条任务、把今天这一段排进今日计划。

校验规则：同一天分配给多个阶段时，各时段时长之和不得超过该任务的每天投入上限；超出或不足都要明确提示。

### 5.5 地图工具

数据：已标记地点（名称、标签、坐标）、路线顺序、每段交通方式、出发时间、平日 / 节假日开关、周边类别与搜索结果。

操作：搜索地点、标记地点、从路线中加入 / 移除地点、调整经过顺序（上移 / 下移）、一键优化顺序、逐段切换交通方式（地铁 / 公交 / 骑行 / 驾车 / 步行）、修改出发时间、切换平日与节假日估算、查询周边（地铁站、打印店、咖啡、餐厅、超市、药店、银行、快递点、便利店、停车场）。

### 5.6 AI 助手

数据：会话与消息、可读取的本机数据范围说明、待确认草稿。

操作：提问、查看会话、展开"它能读到什么"查看具体条目、把结论写进待确认草稿、对模糊目标使用 AI 智能安排（说清目标 → AI 反问 → 生成逐日安排草稿 → 采纳）。

边界：AI 的所有产出先进待确认草稿，未采纳不得写入正式数据；解析失败时原样展示，不猜测、不编造日期。

### 5.7 数据与设置

数据：身份模板与作息（起床、睡觉、默认通勤方式）、常用地址、固定课表与工作表、两把密钥（高德 JS Key 与安全密钥、AI 服务地址 / 模型名 / Key）、外观设置、数据统计。

操作：保存身份与作息、保存密钥、切换明暗与配色、导出备份、导入备份、清空全部数据（清空前自动导出一次）。

---

## 6. 首页、今日计划和各模块之间的关系

### 6.1 主线

智能排布 → 今日计划 → 日历日程 → 任务追踪，四个环节共用同一份数据；地图工具提供地点与路程时间；AI 助手读取以上数据并生成草稿。

### 6.2 关系明细

| 关系 | 说明 |
| --- | --- |
| 首页 → 各模块 | 首页只读取与跳转，不修改其他模块的数据 |
| 首页 ← 各模块 | 即将截止、今天的计划、长周期任务、今天要去的地方均来自对应模块的实时数据 |
| 智能排布 → 今日计划 | 排布结果先成为草稿，采纳后写入当天时间轴 |
| 今日计划 ↔ 任务追踪 | 在今日计划完成一段任务，任务追踪中对应时段自动标记完成、阶段进度同步更新；反向勾选同样生效 |
| 今日计划 → 地图工具 | 当天要去的地点与到达时间要求，决定地图中建议的出发时间 |
| 日历日程 ↔ 任务追踪 | 日历中跨天的条目在任务追踪中形成进程条；任务周期调整后，日历轴线图同步更新 |
| AI 助手 → 各模块 | AI 只产出待确认草稿，采纳后由对应模块写入 |

---

## 7. 本地数据文件、备份和恢复要求

### 7.1 数据文件

数据保存在项目目录的 `data/` 下，按主题分为多个 JSON 文件：

| 文件 | 内容 |
| --- | --- |
| `profile.json` | 身份模板、作息、默认通勤方式 |
| `places.json` | 常用地址（名称、坐标、标签） |
| `routine.json` | 固定课表与工作表 |
| `events.json` | 日历日程（日期、起止时间、类型、标题） |
| `plans.json` | 每日计划（当天的时间轴与完成状态） |
| `tasks.json` | 长周期任务（阶段、时段、每天投入上限、交付日、风险与依赖） |
| `notes.json` | 快速备忘 |
| `pending.json` | 待确认草稿（排布草稿、AI 与文件导入结果） |
| `chats.json` | AI 会话与消息 |
| `settings.json` | 两把密钥、AI 服务信息、外观与界面偏好 |

### 7.2 写入与容错

- 先写临时文件再原子替换，避免写入中断留下半个文件
- 每次替换前保留上一版为 `.bak`
- 启动时校验 JSON，文件损坏时用备份恢复并在界面提示
- 同一文件的并发写入按顺序串行执行

### 7.3 备份与恢复

- 导出备份：生成一个带日期的 JSON 文件，同时写入项目 `backups/` 目录并下载到本机
- 导入备份：整体覆盖现有数据，覆盖前自动把当前数据另存一份快照
- 清空数据：先自动导出一次，再清空

### 7.4 安全与保密

- 密钥只写入本机 `data/settings.json`，不写进代码
- `data/`、`backups/` 加入版本库忽略清单，不提交、不外传
- 主数据不放在浏览器缓存中，避免清理浏览器或更换浏览器导致丢失
- AI Key 与高德安全密钥不出现在页面源码中（高德 JS Key 按平台设计必须出现在页面，仅绑定本机使用）

---

## 8. 第一版必须完成的功能

### 8.1 运行与数据

- [ ] 本机服务启动后，以独立应用窗口打开（`http://localhost:4317`），窗口内不带浏览器地址栏与标签页
- [ ] 窗口最大化启动，铺满屏幕，切换模块时窗口高度不变
- [ ] 全部数据保存到本机文件，刷新、关闭服务、重新启动后不丢失
- [ ] 除地图与 AI 外的功能在断网状态下可用
- [ ] 导出备份、导入备份、清空数据可用

### 8.2 七个模块

- [ ] 首页总览：六个抽屉区块 + 顶部草稿提示，支持单独与全部展开收起
- [ ] 今日计划：草稿采纳、手动添加、删除、勾选完成、按顺序顺延、到点询问与实际用时记录
- [ ] 日历日程：任务轴线图 + 小月历、切换周、新增 / 修改 / 删除日程、重叠标注
- [ ] 任务追踪：进程图、阶段管理、时段编辑、每天投入上限校验、依赖与风险、新建与删除任务
- [ ] 地图工具：地图铺满 + 左右浮层、地点搜索与标记、逐段交通方式、顺序优化、平日 / 节假日估算、周边查询
- [ ] AI 助手：读取本机数据、按模块调整提问、生成待确认草稿、对模糊目标生成逐日安排
- [ ] 数据与设置：身份与作息、两把密钥、明暗与配色、备份与恢复

### 8.3 跨模块联动

- [ ] 今日计划与任务追踪双向同步（勾选完成 ↔ 时段完成、阶段进度）
- [ ] 日历与任务追踪同步（任务周期 ↔ 轴线图与进程条）
- [ ] 首页各区块反映各模块实时数据
- [ ] 地图估算的出发时间进入今日计划与首页

---

## 9. 第一版暂时不做的功能

- 账号、登录、云同步、多设备同步
- 多人协作、共享、评论
- 手机端界面与移动端适配
- 部署上线与服务器托管
- 短信、邮件、微信等外部推送提醒
- 自动抓取网站上的报名信息（只做文件导入）
- 离线地图瓦片下载（地图使用在线地图服务）
- 复杂统计报表与数据大屏
- 除七个模块以外的扩展模块（记账、健康、阅读等）
- 主题深度自定义（第一版只提供五套预设配色与三态明暗）

---

## 10. 可以实际检查的验收标准

1. 双击启动脚本，应用窗口最大化打开（窗口内无地址栏与标签页），顶栏显示当前日期与天气，左侧显示七项导航。
2. 依次点击七项导航，每个模块都能正常显示；整个过程中窗口高度不发生变化。
3. 在数据与设置中修改身份与作息并保存，刷新页面后内容仍在；关闭服务并重新启动后内容仍在。
4. 在数据与设置中切换"亮 / 暗 / 跟随系统"与五套配色，界面立即变化；重启后仍保持上次选择。
5. 在首页快速备忘输入一句话并回车，备忘出现在列表中；刷新页面后仍在；点击删除后消失。
6. 展开任意抽屉，内容以动画展开；点击"折叠所有"后全部收起；再点击"展开所有"后全部展开。
7. 今日计划中出现待确认草稿；点击"采纳"后草稿进入当天时间轴，点击"忽略"后草稿消失。
8. 在今日计划点击"手动加一条"，填写时间与内容后新条目出现在时间轴上；点击该条目的删除按钮后消失。
9. 在今日计划点"按顺序顺延"，该条目与后一条交换先后，时间相应后移；当天已无后续任务时才提示顺延到次日。
10. 在今日计划勾选"整理张同学的文书初稿"完成，任务追踪中"写初稿"阶段对应时段同步变为已完成，阶段进度数字增加；在任务追踪取消勾选，今日计划恢复未完成。
11. 在任务追踪的"每天的安排"中，把同一天两个阶段的时段改到合计超过每天投入上限，该天显示红色"超了 X 小时"；改为等于上限时显示"排满了"。
12. 在任务追踪点"新建任务"，选择阶段模板并填写起止日期后创建，进程图上出现该任务的条，阶段下自动生成若干时段。
13. 在任务追踪点"删除任务"，先出现确认提示并说明会连带移除今日计划中的关联段；确认后任务与关联段一并消失。
14. 在任务追踪点"让 AI 重新安排这条"，出现逐日安排草稿（日期、阶段、时间段）；点"采纳，替换现在的安排"后，该任务的时段被替换。
15. 在日历日程点"上一周 / 下一周"，轴线图切换周；点任意一条日程可修改开始与结束的日期时间，保存后轴线图更新。
16. 在日历中把两条日程改到时间重叠，界面出现"时间重叠 N 分钟"的提示。
17. 在地图工具搜索一个地点并点"标记"，该地点出现在左侧已标记列表中并显示在地图上；点击地点可加入或移出路线。
18. 在地图工具调整经过顺序并点"优化顺序"，出现"X 分钟 → Y 分钟，少走 Z 分钟"的提示；切换"按平日算 / 按节假日算"后总耗时变化。
19. 在任意模块底部的 AI 输入框提问，输入框提示语与快捷问题随模块变化；发送后先显示"正在读本机数据"，随后给出与该模块相关的回答。
20. 在 AI 助手向"它能读到什么"中的条目点击，展开显示具体内容（例如今日计划的六条安排）。
21. AI 生成的安排只出现在待确认草稿中；在未点击采纳前，今日计划与任务追踪的数据不变。
22. 在数据与设置点"导出备份"，得到 `backup-日期.json`，同时 `backups/` 目录出现同名文件。
23. 修改若干数据后点"导入备份"，选择此前导出的文件，数据回到导出时的状态，且 `backups/` 目录出现导入前的快照文件。
24. 断开网络后重新打开应用，首页、今日计划、日历日程、任务追踪、数据与设置均可正常使用；地图工具与 AI 助手提示需要联网。
25. 查看 `data/settings.json`，能看到所填密钥；查看页面源码，看不到 AI Key 与高德安全密钥；`git status` 中不出现 `data/` 与 `backups/` 下的文件。

---

## 附：术语约定

| 术语 | 含义 |
| --- | --- |
| 模块 | 七个一级入口 |
| 任务 | 需要跨天推进、有交付日的一件事 |
| 阶段 | 任务内部的分段，可增加、删除、切换 |
| 时段 | 任务在某一天占用的具体时间，如 20:00-22:00 |
| 每天投入上限 | 一条任务每天最多安排的小时数 |
| 进程条 | 任务追踪中表示任务周期与完成比例的横条 |
| 交付日 | 任务的交付或截止日期，在进程图中以 ◆ 标记 |
| 草稿 | 由智能排布或 AI 生成、等待确认的安排 |
| 采纳 | 使用者确认草稿，将其写入正式数据 |
| 智能排布 | 依据身份、地点、固定课表与截止日期生成安排的核心能力 |
